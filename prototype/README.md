# prototype devcontainer

This folder contains a simple VS Code devcontainer configuration.

How to use:

1. Open the `prototype` folder in VS Code.
2. Use "Reopen in Container" from the Command Palette.

Files added:

- `.devcontainer/devcontainer.json` — devcontainer config using a Python base image
- `requirements.txt` — place Python dependencies here
