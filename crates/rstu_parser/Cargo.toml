[package]
name = "rstu_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1"
rstu_ast = { version = "0.1.0", path = "../rstu_ast" }

[dev-dependencies]
rstest = "0.24"
serde_yaml = "0.9"
